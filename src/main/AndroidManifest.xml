<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mytasks">

    <application
        android:allowBackup="true"
        android:icon="@drawable/formalicon"
        android:label="ِAbdo ELQabany"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.MyTasks">
        <activity android:name=".pray"></activity>
        <activity android:name=".social" />
        <activity
            android:name=".ListTasks"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".welcomeact"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>